// ut_version.h - the one place the mod's version is written. The start-up banner prints it and
// tools\package.ps1 reads it for the zip name, so the two can never disagree.
#pragma once

#define UT_VERSION "1.0.0"
